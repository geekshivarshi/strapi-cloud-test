'use strict';

/**
 * chat-to-kaibot controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::chat-to-kaibot.chat-to-kaibot');
